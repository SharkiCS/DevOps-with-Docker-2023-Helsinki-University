version: '3.8'

services:
    backend:
        image: backend
        container_name: backend
        ports:
            - 8080:8080
        
    frontend:
        image: frontend
        container_name: frontend
        ports:
            - 5001:5001